# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    parent:
        label: Parent Category
        relation: parent
        select: id